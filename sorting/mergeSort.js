function mergeSort(arr, startPoint, endPoint) {
    if(endPoint > startPoint) {

    }

    // mergeSort(arr, startPoint, midPoint);
    // mergeSort(arr, startPoint, midPoint);
    // merge(arr, startPoint, midPoint);
}

function merge() {
    
}